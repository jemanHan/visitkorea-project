# VisitKorea Backend Environment Variables
# Copy this file to apps/backend/.env.local and update the values

# ===========================================
# �� 개인 설정 필수 항목들
# ===========================================

# Google Places API Key (필수) - Google Cloud Console에서 발급
# https://console.cloud.google.com/apis/credentials
GOOGLE_PLACES_BACKEND_KEY=your_google_places_api_key_here

# ===========================================
# ⚙️ 공통 설정 항목들 (수정 불필요)
# ===========================================

# Server Configuration
RUN_TARGET=local
AUTH_PROVIDER=local
PORT=3002

# Database Configuration (모든 팀원 동일)
DATABASE_URL=postgresql://vk:vk@localhost:5432/vk?schema=public

# JWT Secret (기본값 사용, 나중에 EC2에서 통일)
JWT_SECRET=change-me

# Cache Configuration
DETAIL_TTL_MS=604800000

# WAS Server IP (배포 시에만 사용)
WAS_SERVER_IP=13.209.108.148
WAS_SERVER_PORT=3002

# ===========================================
# �� 설정 방법
# ===========================================
# 1. Google Cloud Console에서 Places API 키 발급
# 2. GOOGLE_PLACES_BACKEND_KEY만 수정
# 3. 나머지는 기본값 그대로 사용
# 4. 파일을 apps/backend/.env.local로 복사
