{
  "name": "@db/client",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "prisma/schema.prisma"
  ],
  "scripts": {
    "generate": "prisma generate && node -e \"console.log('Prisma generate 켜져있습니다!')\"",
    "migrate:dev": "prisma migrate dev --name init",
    "studio": "node -e \"console.log('Prisma Studio 켜져있습니다!')\" && prisma studio",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "typescript": "^5.5.4"
  }
}


